import { ListView as BaseListView, ListViewProps } from '@ikepu-tp/react-bootstrap-extender';

export default function ListView(props: ListViewProps): JSX.Element {
	return <BaseListView {...props} />;
}
